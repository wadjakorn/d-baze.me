server {
    listen 80;
    listen 443 ssl;

    ssl_certificate /opt/certs/ssl.crt;
    ssl_certificate_key /opt/certs/ssl.key;

    client_max_body_size 64M;

    root /var/www/html/game01;
    index index.html;

    server_name game01.d-baze.local game01.d-baze.me;

    location / {
        try_files $uri $uri/ /index.html?$args;
    }
}